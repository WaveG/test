ppx:
  environment: @ppx.environment@
  name: service-bank-account
  config:
    publicGroups: middleware

spring:
  application:
    name: test

pingpongx:
  mq:
    namesrvAddr: ${middleware.ons.nameserver}
    accessKey: ${middleware.ons.access-key}
    secretKey: ${middleware.ons.secret-key}
    groupId: test
